{
  "devDependencies": {
    "@parcel/transformer-pug": "^2.2.1",
    "gh-pages": "^3.2.3",
    "parcel": "^2.2.1"
  },
  "scripts": {
    "deploy": "parcel build src/*.pug",
    "watch": "parcel serve src/*.pug"
  }
}